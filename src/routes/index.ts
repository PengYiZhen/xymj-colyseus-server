/**
 * 路由配置
 * 集中管理所有路由相关的配置
 */

/**
 * API 路由前缀
 */
export const API_ROUTE_PREFIX = '/api';

/**
 * 路由配置对象
 */
export const routeConfig = {
  /**
   * API 路由前缀
   */
  apiPrefix: API_ROUTE_PREFIX,
  
  /**
   * 其他路由配置可以在这里添加
   */
} as const;
